Context: This is a Flutter ERP app. I have two files:

language_first_time_popup.dart — a multi-step onboarding popup (Language → Tour Intro)
work_cat.dart — the main landing screen with a WorkType grid, showcase tour, and the popup overlay

I need you to implement a Domain Selection step inside the existing popup flow, and a personalized workspace experience on the home grid — all with production-quality UX. Here are the full requirements:

STEP 1 — Add a 3rd popup panel: Domain/Workspace Selection
After the user taps "Let's Start" or "Explore with us" on the _TourIntroBody panel, do not dismiss the popup yet. Instead, animate in a new panel (_DomainSelectionBody) inside the same _PopupShell, using the existing AnimatedSwitcher slide transition pattern already in place.
_DomainSelectionBody requirements:

Title: "What's your work domain?", subtitle: "Select the work types you handle. You can always add more later."
Display all WorkType.values as a multi-select chip grid (2 columns). Each chip shows workType.imagePath as a small thumbnail (ClipRRect, ~48×48), workType.displayName, and a checkmark overlay when selected. Use the existing CompanyCard accent colors as the chip border/highlight color when selected.
A "Start My Workspace" primary button (disabled until ≥1 selected) and a "Skip, show everything" text button below it.
Animate chip selection with AnimatedContainer scale + border color transition.
Store the selected WorkType names in a List<String> via SharedPreferences key 'user_workspace_types'. If skipped, store an empty list (meaning "show all").
After confirming, call widget.onStartTour (existing callback) and dismiss the popup — passing the selected list back through a new required callback: void Function(List<String> selectedTypes) onWorkspaceConfirmed.

Panel routing inside _LanguageFirstTimePopupState:

_activePanel == 0 → _LanguageBody
_activePanel == 1 → _TourIntroBody
_activePanel == 2 → _DomainSelectionBody

When "Let's Start" / "Explore with us" is tapped on panel 1, set _activePanel = 2. The existing AnimatedSwitcher with slide transition handles the animation automatically.

STEP 2 — Workspace-aware grid in WorkCategoryScreen
Add a List<String> _workspaceTypes = [] state variable. Populate it from SharedPreferences 'user_workspace_types' in initState (after the language popup check). An empty list means "user skipped" = show all normally.
Grid rendering logic:
Case A — User selected specific types (list not empty):

Selected cards render fully as today, in their grid positions.
Non-selected cards render as a visually collapsed + frosted tile: blurred image (ImageFilter.blur), reduced opacity (0.38), a small lock/add icon, and a label "Tap to add to workspace". These tiles are still tappable — tapping one shows a bottom sheet or inline SnackBar: "Added [WorkType] to your workspace!" and updates _workspaceTypes + SharedPreferences. Animate the card from collapsed → full using AnimatedSwitcher or TweenAnimationBuilder.
Show a small persistent "+ Add more" FAB or inline chip at the bottom of the grid: "Manage workspace" → navigates to a simple full-screen selector (reuse _DomainSelectionBody logic as a standalone screen or modal).

Case B — User skipped (empty list):

All cards render fully as today (no change to existing behavior).
First tap on any card → show a non-blocking SnackBar with "Tip: You can personalize your workspace from Settings → Workspace." (show only once, gate with a SharedPreferences bool 'workspace_tip_shown').

Showcase/tour integration:

The existing TourRegistry.workCategoryKey showcase should only attach to workspace-visible cards (index 0 of the filtered list, not always the first WorkType).
If user has a workspace, the tour description should say "These are your selected work domains" instead of the generic message.


STEP 3 — General UX polish rules (treat user as a first-time newcomer)

All state transitions must use AnimatedSwitcher, AnimatedContainer, or TweenAnimationBuilder. No abrupt layout jumps.
Collapsed/blurred cards must maintain their grid slot size — do not reflow the grid when a card is added. Use AnimatedOpacity + AnimatedScale on the card content only, not the grid cell.
The popup's back navigation (panel 2 → panel 1) should be supported: add a subtle back arrow in _DomainSelectionBody header that decrements _activePanel.
Tapping "Skip, show everything" on the domain panel must feel intentional — add a brief 200ms delay + a ripple confirm before dismissing, so users don't accidentally skip.
All SharedPreferences reads/writes must be wrapped in try/catch. On read failure, default to "show all" gracefully.
Do not break the existing showcase/tour gating logic (_languageFlowDone, _tourIntroSkipped, _showcaseStarted flags). The domain selection panel is part of the language/onboarding flow — set _languageFlowDone = true only after the popup fully dismisses (panel 2 confirm or skip).
Preserve all existing callbacks: onSelectLanguage, onSkip, onStartTour, onSkipTour. Add only onWorkspaceConfirmed as a new required callback on LanguageFirstTimePopup.


Files to modify:

language_first_time_popup.dart — add _DomainSelectionBody, new panel routing, onWorkspaceConfirmed callback
work_cat.dart — workspace-aware grid rendering, collapsed card widget, "add to workspace" interaction, FAB/chip for workspace management, updated showcase key targeting

Do NOT change:

Routing/navigation logic (go_router, Routes)
Auth/provider logic
TourRegistry, TourController
Existing LanguagePopupPrefs, LanguageStorage, languageRepositoryProvider

